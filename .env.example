# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-super-secret-key-change-this-in-production

# Google OAuth Configuration
# Get these credentials from Google Cloud Console (console.cloud.google.com)
# See GOOGLE_OAUTH_SETUP.md for detailed setup instructions
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# PostgreSQL Database Configuration (Digital Ocean Managed Database)
# See DATABASE_SETUP.md for detailed setup instructions

# Connection pooling URL (use this for Prisma queries)
# Format: postgresql://username:password@host:port/database?schema=public&sslmode=require&pgbouncer=true&connection_limit=1
DATABASE_URL="postgresql://username:password@host:port/database?schema=public&sslmode=require&pgbouncer=true&connection_limit=1"

# Direct connection URL (use this for migrations)
# Format: postgresql://username:password@host:port/database?schema=public&sslmode=require
DIRECT_DATABASE_URL="postgresql://username:password@host:port/database?schema=public&sslmode=require"

# Connection Pool Settings (Optional - for production optimization)
# DATABASE_POOL_TIMEOUT=10
# DATABASE_POOL_MIN_SIZE=2
# DATABASE_POOL_MAX_SIZE=10

# Demo Accounts (for development)
# Client: demo@bookedsolid.ai / DemoClient2025!
# Admin: admin@bookedsolid.ai / AdminAccess2025!

# Retell.ai Webhook Configuration
# Get this from: https://dashboard.retellai.com/settings/webhooks
# This secret is used to verify webhook signatures
RETELL_WEBHOOK_SECRET="your-retell-webhook-secret-here"
