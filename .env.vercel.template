# Vercel + Supabase Environment Variables Template
# Add these in Vercel Dashboard: Settings > Environment Variables

# =============================================================================
# DATABASE - SUPABASE (Free Tier: 500MB, 2GB bandwidth, 50MB file storage)
# =============================================================================

# Get these from Supabase Dashboard > Project Settings > Database
# Connection Pooler (Recommended for Vercel serverless)
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@[PROJECT-REF].pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"

# Direct Connection (for migrations only)
DIRECT_DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# =============================================================================
# NEXTAUTH (Authentication)
# =============================================================================

# Generate: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-here"

# Auto-set by Vercel (use production domain or vercel URL)
NEXTAUTH_URL="https://your-app.vercel.app"

# =============================================================================
# RETELL.AI (Voice AI Integration)
# =============================================================================

# Get from Retell Dashboard: https://app.retellai.com
RETELL_WEBHOOK_SECRET="your-retell-webhook-secret"
RETELL_API_KEY="your-retell-api-key"

# =============================================================================
# OPTIONAL: GOOGLE OAUTH
# =============================================================================

# GOOGLE_CLIENT_ID="your-google-client-id"
# GOOGLE_CLIENT_SECRET="your-google-client-secret"

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================

# 1. Supabase Free Tier Limits:
#    - 500MB database size
#    - 2GB bandwidth/month
#    - 50,000 monthly active users
#    - Paused after 7 days inactivity (auto-resumes)
#
# 2. Vercel Free Tier Limits:
#    - 100GB bandwidth/month
#    - 100 deployments/day
#    - 6,000 build minutes/month (600 hours)
#    - Serverless function: 10s execution limit
#
# 3. Connection Pooling:
#    - Always use pooler URL for DATABASE_URL (required for Vercel)
#    - Use direct URL only for DIRECT_DATABASE_URL (migrations)
#    - Limit connections: &connection_limit=1 (important!)
#
# 4. First Deploy:
#    - Run migrations: vercel env pull .env.local && npx prisma migrate deploy
#    - Or use build command: prisma migrate deploy && next build
#
# 5. Webhook URLs:
#    - Retell: https://your-app.vercel.app/api/webhooks/retell
#    - Update in Retell dashboard after first deploy
